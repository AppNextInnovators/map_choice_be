# Memory Index


## Rules (auto-loaded from .claude/rules/)

- `.claude/rules/git.md` — git/PR rules (branch naming, no force-push, PRs target dev)
- `.claude/rules/file_placement.md` — all files must use repo-relative paths, never absolute paths
